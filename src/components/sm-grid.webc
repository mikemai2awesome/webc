<div webc:root is-stack>
  <slot></slot>
</div>

<style>
  sm-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(20ch, 1fr));
    gap: var(--gutter);
  }

  sm-grid > * {
    margin: 0;
  }
</style>
