<div webc:root is-stack>
  <slot></slot>
</div>

<style>
  sm-flex {
    display: flex;
    flex-wrap: wrap;
    gap: var(--gutter);
    row-gap: var(--stack);
  }

  sm-flexitem {
    flex: 1 1 auto;
    flex-basis: var(--min-inline-size);
    align-items: center;
    justify-content: center;
    margin: 0;
  }

  sm-flex[is-box] {
    row-gap: var(--gutter);
  }
</style>
